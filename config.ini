[Credenziali]
RAGIONE_SOCIALE = DHL EXPRESS ITALY
CODICE_FISCALE = 04209680158
EMAIL = itcustoms.mxbhub@dhl.com

[Impostazioni]
# Imposta headless = true per nascondere il browser durante l'esecuzione e renderla più veloce.
# Imposta headless = false per visualizzare il browser (utile per il debug).
HEADLESS = true

# Timeout in millisecondi per le operazioni standard (default: 120000 = 2 minuti)
TIMEOUT_DEFAULT = 120000

# Timeout in millisecondi per selettori critici come #codiceTariffa (default: 180000 = 3 minuti)
TIMEOUT_SELETTORE_CRITICO = 180000

# Numero massimo di tentativi in caso di timeout (default: 3)
MAX_RETRY = 3

# Delay in secondi dopo che lo spinner scompare, prima di cercare elementi (default: 2)
WAIT_AFTER_SPINNER = 2 
